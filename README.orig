
Build Instructions:

svn co svn://svn.openwrt.org/openwrt/trunk/ openwrt
cd openwrt
./scripts/feeds update -a
./scripts/feeds install -a
<<<<<<< HEAD
git clone git@github.com:unitedwifi/freifunk-unitedwifi.git
mv freifunk-unitedwifi/package/* package/
mv freifunk-unitedwifi/files .
rm -rf freifunk-unitedwifi
=======
git clone https://github.com/freifunk-bielefeld/firmware.git
mv firmware/package/* package/
mv firmware/files ./
rm -rf firmware
>>>>>>> upstream/master
make defconfig
make menuconfig

# Now select the right Target System and Target Profile:
# For the DIR-300:
#  Target System => Atheros AR231x
#  Target Profile => Default
# For the TL-WR841ND:
#  Target System => Atheros AR7xxx
#  Target Profile => TP-LINK TL-WR841ND
# Many other routers have not been tested yet
# but may work. Give it a try! :-)

make

#the firmware images are now in the bin folder
